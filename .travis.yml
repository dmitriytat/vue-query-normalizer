language: node_js
node_js:
  - stable
script:
  - npm run build
  - npm run test:coveralls
deploy:
  - provider: npm
    email: $NPM_EMAIL
    api_key: $NPM_KEY
    skip_cleanup: true
    on:
      tags: true
